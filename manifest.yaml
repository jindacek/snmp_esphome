name: snmp_sensor
version: 1.1.0

build:
  include:
    - components/snmp_sensor
  libraries:
    - staianoweb/Arduino SNMP

dependencies:
  esphome:
    components:
      - sensor
      - text_sensor
      - binary_sensor
